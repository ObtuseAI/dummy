# NOT_APPROVAL — operator env TEMPLATE. Editing this does not authorize anything.
# Fill placeholders, save as a real .env, then `source` it. Dummy still enforces every gate.
DUMMY_OPERATOR_NAME="<operator name>"
DUMMY_OPERATOR_REASON="<reason for one controlled production pilot>"
DUMMY_OPERATOR_EXPIRES_AT="<ISO-8601 expiry timestamp>"
DUMMY_AUTHORITY_PACK_DIR="operator_authority_pack"
DUMMY_TYPED_APPROVAL="I approve Dummy to run one controlled production pilot through LiveBrokerFirewall only, with no market orders, strict caps, live-submit already operator-enabled, per-order fail-closed checks, and immediate pilot auto-lock"
DUMMY_RISK_ACK="I understand this can place one real limit order only through LiveBrokerFirewall after all Dummy gates pass"
# DUMMY_AUTHORITY_INSTALL_CONFIRM="I authorize installing these operator-created authority files into Dummy runtime"
# DUMMY_LIVE_PROOF_MODE=1
# DUMMY_LIVE_PROOF_ACK=FULL_AUTHORITY_OPERATOR_APPROVED_LIVE_PROOF_ONLY
