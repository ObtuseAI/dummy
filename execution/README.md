# Protected execution namespace

This namespace is intentionally code-free. Live-order submission remains behind
`live_firewall/`, explicit operator authority, and the immutable caps and
live-submit contracts.

The directory exists only to reserve the constitutionally protected
`execution/` namespace. Recursive research and generated candidates may not add
or mutate execution adapters here.
